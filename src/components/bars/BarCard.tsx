import React from 'react';

import Bar from '../../services/structureMethods/Bar';

interface Props {
  bar: Bar;
}

const BarCard = (props: Bar) => {
  return (
    <div>
      
    </div>
  );
}

export default BarCard;
